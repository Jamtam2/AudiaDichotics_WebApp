syntax = "proto3";

package transcription;


service TranscriptionService {
  rpc Transcribe (AudioChunk) returns (TranscriptionResult);
}


message AudioChunk {
  bytes audio_data = 1;
}

message TranscriptionResult {
  string transcript = 1;
}
